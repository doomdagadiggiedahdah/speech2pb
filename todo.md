# stt_hk todo

- [ ] profile the full pipeline (record → atempo → whisper → GPT → clipboard) to find bottlenecks
- [ ] test keeping the small model constantly loaded (e.g. as a daemon/socket server) to skip model load time on each invocation
